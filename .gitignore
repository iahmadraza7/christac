# Secrets. Never committed; the host gets these as environment variables.
.env

# Build and run artefacts
__pycache__/
results/

# The running spend ledger, written at runtime. Not source.
server/.spend.json
